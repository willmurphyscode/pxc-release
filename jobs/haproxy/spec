---
name: haproxy

templates:
  bpm.yml.erb: config/bpm.yml
  haproxy.cfg.erb: config/haproxy.cfg

packages:
- haproxy

properties:
  replication.leader_address:
    description: "The IP address of the leader MySQL instance"
